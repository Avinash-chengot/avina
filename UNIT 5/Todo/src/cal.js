// add function

function add(first, second) {
    // take first and second returns addition

    return first + second;
}

function mult(first, second) {
    // take first and second returns addition

    return first * second;
}




module.exports={
    add,
    mult
}